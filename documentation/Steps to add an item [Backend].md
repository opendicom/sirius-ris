Schema
Routes
isDuplicated [REVISION FINAL NECESARIA]
Importar schema y rutas al main server
Handlers
adjustDataTypes (aggregation cases)
checkReferences (if this element is referenced with fk in another element)
addDomainCondition
Main Permissions

[REVISION FINAL NECESARIA]
Reglas de validación y normalización de express validator.
